# Agent máy đích: tiếp nhận và nghiệm thu PrismaFlex 1.2.2

**Yêu cầu này được thay thế bởi `2026-09-23-update-1.2.3.md`. Dùng bộ 1.2.3 và hướng dẫn mới.**

Bộ cài và tài liệu được chuẩn bị từ nhánh sửa thời gian/chart/journal. Đọc `releases/1.2.2/START-HERE.md` và báo cáo review trước khi thao tác.

Thực hiện trong cửa sổ bảo trì phù hợp: kiểm kê bản cũ, xác minh SHA/chữ ký/license/site/revision, cập nhật bằng updater cũ, kiểm tra readiness và dữ liệu sau cập nhật. Không mở thêm Collector. Không tự duyệt hoặc xóa treatment để vượt lỗi.

Giai đoạn đầu chấp nhận không nối liền treatment đang chạy qua update; ưu tiên ứng dụng cập nhật được và tiếp tục nhận dữ liệu/chart/journal bình thường. Vẫn giữ backup và ca cũ để tra cứu.

Các kiểm tra chưa chạy phải đánh dấu CHƯA CHẠY. Không ghi PASS chỉ vì trang mở được. Không thử ngắt thiết bị điều trị thật để kiểm tra mất kết nối.

Trả báo cáo đã khử định danh vào `reports/2026-09-23-update-1.2.2-target.md`: phiên bản trước/sau, SHA gói, backup ID, trạng thái update/services/license, giờ card/chart, dữ liệu live/reconnect và journal. Repo này công khai: không đưa mã bệnh nhân, raw packet, DB dump, runtime config hoặc license vào báo cáo.
